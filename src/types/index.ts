/**
 * Re-exports all share-related type definitions (e.g. SharePlatform, ShareIconKey).
 * 
 * Centralizes type usage for sharing logic and fallback UI.
 */

export * from './share.types';

/**
 * Re-exports all internationalization-related types (e.g. LangKeys).
 * 
 * Used for localization logic across the component.
 */

export * from './i18n.types';